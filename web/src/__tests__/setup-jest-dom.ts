/**
 * Pulls in @testing-library/jest-dom matchers (toBeInTheDocument, toHaveClass,
 * toHaveTextContent, toHaveAttribute, etc.). Loaded via setupFilesAfterEach in
 * `jest.config.js`.
 */
import "@testing-library/jest-dom";
